<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mybatis.mapper.BookMapper">
  <select id="findAllBooks" resultType="com.example.mybatis.entity.Book">
    SELECT * FROM books ORDER BY id
  </select>

  <select id="getBookById" resultType="com.example.mybatis.entity.Book">
    SELECT * FROM books WHERE id = #{id}
  </select>

  <insert id="save" parameterType="com.example.mybatis.entity.Book">
    insert into books (title , author) values (#{title} , #{author})
  </insert>

  <update id="update" parameterType="com.example.mybatis.entity.Book">
    update books set title = #{title} , author = #{author} where id = #{id}
  </update>

  <delete id="delete" parameterType="int">
    delete from books where id = #{id}
  </delete>
</mapper>
